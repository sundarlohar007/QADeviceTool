<Window x:Class="LogPro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:LogPro"
        xmlns:vm="clr-namespace:LogPro.ViewModels"
        xmlns:views="clr-namespace:LogPro.Views"
        xmlns:conv="clr-namespace:LogPro.Converters"
        Title="QADeviceTool"
        Width="1280" Height="800"
        MinWidth="960" MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Background="{x:Null}"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResize"
        Icon="QAQCDeviceIcon.ico">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="4" CornerRadius="0" GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Window.Style>
        <Style TargetType="Window">
            <Style.Triggers>
                <Trigger Property="WindowState" Value="Maximized">
                    <Setter Property="Padding" Value="0" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Style>

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}"><views:DashboardView /></DataTemplate>
        <DataTemplate DataType="{x:Type vm:SessionViewModel}"><views:SessionView /></DataTemplate>
        <DataTemplate DataType="{x:Type vm:DeviceViewModel}"><views:DeviceView /></DataTemplate>
        <DataTemplate DataType="{x:Type vm:AppManagementViewModel}"><views:AppManagementView /></DataTemplate>
        <DataTemplate DataType="{x:Type vm:ShellViewModel}"><views:ShellView /></DataTemplate>
        <DataTemplate DataType="{x:Type vm:DeepLinkViewModel}"><views:DeepLinkView /></DataTemplate>
        <DataTemplate DataType="{x:Type vm:VitalsViewModel}"><views:VitalsView /></DataTemplate>
        <DataTemplate DataType="{x:Type vm:FileExplorerViewModel}"><views:FileExplorerView /></DataTemplate>
        <DataTemplate DataType="{x:Type vm:MacroViewModel}"><views:MacroView /></DataTemplate>
        <DataTemplate DataType="{x:Type vm:StressTestViewModel}"><views:StressTestView /></DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}"><views:SettingsView /></DataTemplate>

        <Style x:Key="NavItem" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{DynamicResource BrushTextSecondary}" />
            <Setter Property="FontFamily" Value="{StaticResource FontHeader}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="bd" Background="Transparent" BorderBrush="Transparent" BorderThickness="2,0,0,0" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource BrushElevated}" />
                                <Setter Property="Foreground" Value="{DynamicResource BrushPrimaryLight}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource BrushSurface}" />
                                <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource BrushPrimaryLight}" />
                                <Setter Property="Foreground" Value="{DynamicResource BrushPrimaryLight}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WinCtrl" TargetType="Button">
            <Setter Property="Width" Value="46" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource BrushTextSecondary}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{DynamicResource BrushElevated}" />
                                <Setter Property="Foreground" Value="{DynamicResource BrushTextPrimary}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseCtrl" TargetType="Button" BasedOn="{StaticResource WinCtrl}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#E81123" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

<Border Background="{DynamicResource BrushVoid}" BorderBrush="{DynamicResource BrushBase}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
                <RowDefinition Height="28" />
            </Grid.RowDefinitions>

            <!-- TITLE BAR -->
            <Border Grid.Row="0" Background="{DynamicResource BrushBase}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="16,0" VerticalAlignment="Center">
                        <TextBlock Text="QADeviceTool" FontFamily="{StaticResource FontHeader}" FontSize="14" FontWeight="Bold"
                                   Foreground="{DynamicResource BrushTextPrimary}" VerticalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Style="{StaticResource WinCtrl}" Content="&#xE921;" Click="Minimize_Click" />
                        <Button Style="{StaticResource WinCtrl}" Content="&#xE922;" Click="Maximize_Click" />
                        <Button Style="{StaticResource CloseCtrl}" Content="&#xE8BB;" Click="Close_Click" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- MAIN AREA -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- SIDEBAR -->
                <Border Grid.Column="0" Background="{DynamicResource BrushBase}">
                    <DockPanel>
                        <!-- Brand at top -->
                        <StackPanel DockPanel.Dock="Top" Margin="16,16,16,24">
                            <TextBlock Text="QADeviceTool" FontFamily="{StaticResource FontHeader}" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource BrushPrimaryLight}" />
                            <TextBlock Text="Terminal v3.0" FontFamily="{StaticResource FontHeader}" FontSize="12" FontWeight="Medium" Foreground="{DynamicResource BrushTextSecondary}" Opacity="0.6" Margin="0,4,0,0" />
                        </StackPanel>

                        <!-- Collapse at bottom -->
                        <Button DockPanel.Dock="Bottom" Margin="16,8" Background="Transparent" Foreground="{DynamicResource BrushTextSecondary}" BorderThickness="0" Cursor="Hand"
                                Command="{Binding ToggleSidebarCommand}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="b" Background="{TemplateBinding Background}" Padding="12,8" CornerRadius="6">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="&#xE700;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,8,0" />
                                            <TextBlock Text="Collapse" FontFamily="{StaticResource FontHeader}" FontSize="12" FontWeight="Medium" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="b" Property="Background" Value="{DynamicResource BrushElevated}" />
                                            <Setter Property="Foreground" Value="{DynamicResource BrushPrimaryLight}" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Nav items -->
                        <StackPanel>
                            <RadioButton Style="{StaticResource NavItem}" IsChecked="{Binding SelectedNavItem, Converter={x:Static conv:StringEqualsConverter.Instance}, ConverterParameter=dashboard}" Command="{Binding NavigateCommand}" CommandParameter="Dashboard">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE80F;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,12,0" />
                                    <TextBlock Text="Dashboard" VerticalAlignment="Center" />
                                </StackPanel>
                            </RadioButton>
                            <RadioButton Style="{StaticResource NavItem}" IsChecked="{Binding SelectedNavItem, Converter={x:Static conv:StringEqualsConverter.Instance}, ConverterParameter=sessions}" Command="{Binding NavigateCommand}" CommandParameter="Sessions">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE7C3;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,12,0" />
                                    <TextBlock Text="Sessions" VerticalAlignment="Center" />
                                </StackPanel>
                            </RadioButton>
                            <RadioButton Style="{StaticResource NavItem}" Command="{Binding NavigateCommand}" CommandParameter="Device">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE8EA;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,12,0" />
                                    <TextBlock Text="Devices" VerticalAlignment="Center" />
                                </StackPanel>
                            </RadioButton>
                            <RadioButton Style="{StaticResource NavItem}" Command="{Binding NavigateCommand}" CommandParameter="Files">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE838;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,12,0" />
                                    <TextBlock Text="File Explorer" VerticalAlignment="Center" />
                                </StackPanel>
                            </RadioButton>
                            <RadioButton Style="{StaticResource NavItem}" Command="{Binding NavigateCommand}" CommandParameter="Apps">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xECAA;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,12,0" />
                                    <TextBlock Text="Apps" VerticalAlignment="Center" />
                                </StackPanel>
                            </RadioButton>
                            <RadioButton Style="{StaticResource NavItem}" Command="{Binding NavigateCommand}" CommandParameter="Shell">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE756;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,12,0" />
                                    <TextBlock Text="Shell" VerticalAlignment="Center" />
                                </StackPanel>
                            </RadioButton>
                            <RadioButton Style="{StaticResource NavItem}" Command="{Binding NavigateCommand}" CommandParameter="DeepLink">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE71B;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,12,0" />
                                    <TextBlock Text="Deep Link" VerticalAlignment="Center" />
                                </StackPanel>
                            </RadioButton>
                            <RadioButton Style="{StaticResource NavItem}" IsChecked="{Binding SelectedNavItem, Converter={x:Static conv:StringEqualsConverter.Instance}, ConverterParameter=vitals}" Command="{Binding NavigateCommand}" CommandParameter="Vitals">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE9D9;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,12,0" />
                                    <TextBlock Text="Vitals" VerticalAlignment="Center" />
                                </StackPanel>
                            </RadioButton>
                            <RadioButton Style="{StaticResource NavItem}" Command="{Binding NavigateCommand}" CommandParameter="Macros">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE756;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,12,0" />
                                    <TextBlock Text="Macros" VerticalAlignment="Center" />
                                </StackPanel>
                            </RadioButton>
                            <RadioButton Style="{StaticResource NavItem}" Command="{Binding NavigateCommand}" CommandParameter="StressTest">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xEC4A;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,12,0" />
                                    <TextBlock Text="Stress Test" VerticalAlignment="Center" />
                                </StackPanel>
                            </RadioButton>
                            <RadioButton Style="{StaticResource NavItem}" Command="{Binding NavigateCommand}" CommandParameter="Settings">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,12,0" />
                                    <TextBlock Text="Settings" VerticalAlignment="Center" />
                                </StackPanel>
                            </RadioButton>
                        </StackPanel>
                    </DockPanel>
                </Border>

                <!-- CONTENT -->
                <Border Grid.Column="1" Background="{DynamicResource BrushVoid}">
                    <ContentPresenter Content="{Binding CurrentView}" />
                </Border>
            </Grid>

            <!-- STATUS BAR -->
            <Border Grid.Row="2" Background="{DynamicResource BrushSurface}">
                <Grid Margin="16,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Ellipse Width="6" Height="6" Margin="0,0,8,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="#EF4444" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ConnectedDeviceCount, Converter={x:Static conv:IntToBoolConverter.Instance}}" Value="True">
                                            <Setter Property="Fill" Value="#4ADE80" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Text="SYSTEM STATUS: NOMINAL" FontFamily="{StaticResource FontHeader}" FontSize="12" FontWeight="Medium" Foreground="{DynamicResource BrushTextSecondary}" VerticalAlignment="Center" Margin="0,0,24,0" />
                        <TextBlock FontFamily="{StaticResource FontHeader}" FontSize="12" FontWeight="Medium" Foreground="{DynamicResource BrushTextSecondary}" VerticalAlignment="Center" Margin="0,0,24,0">
                            <Run Text="Devices: " /><Run Text="{Binding ConnectedDeviceCount}" />
                        </TextBlock>
                        <TextBlock Text="{Binding StatusBarText}" FontFamily="{StaticResource FontHeader}" FontSize="12" FontWeight="Medium" Foreground="{DynamicResource BrushTextSecondary}" VerticalAlignment="Center" />
                    </StackPanel>
                    <TextBlock Grid.Column="1" Text="v3.0.0" FontFamily="{StaticResource FontMono}" FontSize="12" FontWeight="Medium" Foreground="{DynamicResource BrushPrimaryLight}" VerticalAlignment="Center" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>