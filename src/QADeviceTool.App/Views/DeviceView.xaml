<UserControl x:Class="LogPro.Views.DeviceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:LogPro.ViewModels"
             xmlns:conv="clr-namespace:LogPro.Converters">
    <UserControl.Resources>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="#1A1A1F" />
            <Setter Property="BorderBrush" Value="#252530" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="12" />
        </Style>
        <Style x:Key="CardHdr" TargetType="Border">
            <Setter Property="Background" Value="#22222A" />
            <Setter Property="Padding" Value="16,12" />
            <Setter Property="CornerRadius" Value="12,12,0,0" />
        </Style>
        <Style x:Key="HdrText" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource FontHeader}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="#E5E2E1" />
        </Style>
        <Style x:Key="ActBtn" TargetType="Button">
            <Setter Property="Background" Value="#1A1A1F" />
            <Setter Property="Foreground" Value="#BAC9CD" />
            <Setter Property="BorderBrush" Value="#3C494C" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontFamily" Value="{StaticResource FontHeader}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Medium" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="BorderBrush" Value="#8CEBFF" />
                                <Setter Property="Foreground" Value="#8CEBFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Background="#060606">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="24,24,24,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Devices" FontFamily="{StaticResource FontHeader}" FontSize="24" FontWeight="SemiBold" Foreground="#E5E2E1" />
            <Button Grid.Column="1" Style="{StaticResource ActBtn}" Command="{Binding RefreshDevicesCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,8,0" />
                    <TextBlock Text="Refresh" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </Grid>
        <Grid Grid.Row="1" Margin="24,0,24,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{StaticResource Card}">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Style="{StaticResource CardHdr}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE8EA;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="#8CEBFF" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="CONNECTED DEVICES" Style="{StaticResource HdrText}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <TextBlock Text="No devices detected. Connect via USB." Foreground="#859397" FontSize="12" HorizontalAlignment="Center" Margin="0,24,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Devices.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <ListBox ItemsSource="{Binding Devices}" SelectedItem="{Binding SelectedDevice}" Background="Transparent" BorderThickness="0" Padding="8">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="8" />
                                <Setter Property="Margin" Value="0,2" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="b" Background="#252530" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="b" Property="Background" Value="#003848" />
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="b" Property="Background" Value="#2A2A33" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Width="32" Height="32" Background="#353534" CornerRadius="6" Margin="0,0,12,0">
                                        <TextBlock Text="&#xE8EA;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="#BAC9CD" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding DisplayName}" FontFamily="{StaticResource FontBody}" FontSize="13" FontWeight="SemiBold" Foreground="#E5E2E1" />
                                        <TextBlock Text="{Binding Serial}" FontFamily="{StaticResource FontMono}" FontSize="10" Foreground="#859397" />
                                    </StackPanel>
                                    <Ellipse Grid.Column="2" Width="8" Height="8" Fill="#4ADE80" VerticalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource Card}">
                <DockPanel>
                    <Border DockPanel.Dock="Top" Style="{StaticResource CardHdr}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="#8CEBFF" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <TextBlock Text="DEVICE DETAILS" Style="{StaticResource HdrText}" VerticalAlignment="Center" />
                        </StackPanel>
                    </Border>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
                        <StackPanel>
                            <TextBlock Text="Select a device to view details." Foreground="#859397" FontSize="12" HorizontalAlignment="Center" Margin="0,24,0,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Visible" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedDevice, Converter={x:Static conv:NullToBoolConverter.Instance}}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <StackPanel Visibility="{Binding SelectedDevice, Converter={x:Static conv:NullToBoolConverter.Instance}, ConverterParameter=Vis}">
                                <TextBlock Text="{Binding SelectedDevice.DisplayName}" FontFamily="{StaticResource FontHeader}" FontSize="20" FontWeight="Bold" Foreground="#E5E2E1" Margin="0,0,0,4" />
                                <TextBlock Text="{Binding SelectedDevice.StatusText}" FontFamily="{StaticResource FontHeader}" FontSize="11" FontWeight="Medium" Foreground="#4ADE80" Margin="0,0,0,16" />
                                <TextBox Text="{Binding DeviceDetails}" FontFamily="{StaticResource FontMono}" FontSize="11" Foreground="#E5E2E1" Background="#0E0E0E" BorderBrush="#3C494C" BorderThickness="1" Padding="12" IsReadOnly="True" TextWrapping="NoWrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" MinHeight="200" />
                                <WrapPanel Margin="0,16,0,16">
                                    <Button Style="{StaticResource ActBtn}" Margin="0,0,8,8" Command="{Binding StartMirrorCommand}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="&#xE7F4;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,6,0" />
                                            <TextBlock Text="Mirror" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource ActBtn}" Margin="0,0,8,8" Command="{Binding StopMirrorCommand}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="&#xE71A;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,6,0" />
                                            <TextBlock Text="Stop" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource ActBtn}" Margin="0,0,8,8" Command="{Binding TakeSnapshotCommand}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="&#xE722;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,6,0" />
                                            <TextBlock Text="Snapshot" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </Button>
                                    <Button Style="{StaticResource ActBtn}" Margin="0,0,0,8" Command="{Binding EnableWirelessCommand}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="&#xE701;" FontFamily="Segoe MDL2 Assets" FontSize="14" Margin="0,0,6,0" />
                                            <TextBlock Text="Wireless" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </Button>
                                </WrapPanel>
                                <TextBlock Text="NOTES" FontFamily="{StaticResource FontHeader}" FontSize="10" FontWeight="Bold" Foreground="#859397" Margin="0,8,0,4" />
                                <TextBox Text="{Binding DeviceNotes, UpdateSourceTrigger=PropertyChanged}" Background="#0E0E0E" Foreground="#E5E2E1" BorderBrush="#3C494C" BorderThickness="1" Padding="8" TextWrapping="Wrap" AcceptsReturn="True" Height="60" />
                                <Button Style="{StaticResource ActBtn}" HorizontalAlignment="Left" Margin="0,8,0,0" Command="{Binding SaveDeviceNotesCommand}" Content="Save Notes" />
                                <TextBlock Text="{Binding StatusMessage}" Foreground="#8CEBFF" FontSize="11" Margin="0,12,0,0" />
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>