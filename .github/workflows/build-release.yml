name: Build & Release

on:
  push:
    branches: [main, beta]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version override (e.g. 3.2.1)'
        required: false

env:
  SOLUTION: LogPro.sln
  PROJECT: src/LogPro.App/LogPro.App.csproj
  CLI_PROJECT: src/LogPro.Cli/LogPro.Cli.csproj
  TEST_PROJECT: src/LogPro.Tests/LogPro.Tests.csproj
  SETUP_SCRIPT: publish/setup.iss

permissions:
  contents: write
  actions: read

jobs:
  build:
    name: Publish
    runs-on: windows-latest
    outputs:
      version: ${{ steps.version.outputs.semver }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          lfs: true

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore ${{ env.SOLUTION }}

      - name: Build
        run: dotnet build ${{ env.SOLUTION }} -c Release --no-restore

      - name: Test
        run: dotnet test ${{ env.TEST_PROJECT }} -c Release --no-build --verbosity normal

      - name: Determine Version
        id: version
        shell: pwsh
        env:
          VERSION_OVERRIDE: ${{ inputs.version }}
        run: |
          if ($env:VERSION_OVERRIDE) {
            if ($env:VERSION_OVERRIDE -notmatch '^\d+\.\d+\.\d+$') {
              throw "Version override must be strict semver, for example 3.2.115."
            }
            $ver = $env:VERSION_OVERRIDE
          } else {
            $ver = "3.2.${{ github.run_number }}"
          }
          echo "semver=$ver" >> $env:GITHUB_OUTPUT
          Write-Host "Version: $ver"

      - name: Publish App
        run: dotnet publish ${{ env.PROJECT }} -c Release -r win-x64 --self-contained true -p:PublishSingleFile=false -p:Version=${{ steps.version.outputs.semver }} -o publish/app

      - name: Validate Windows package entry point
        shell: pwsh
        run: |
          $exe = Join-Path "publish/app" "LogPro.exe"
          if (-not (Test-Path -LiteralPath $exe -PathType Leaf)) {
            throw "Windows publish did not produce LogPro.exe. Check LogPro.App.csproj AssemblyName."
          }

          $setup = Get-Content -LiteralPath $env:SETUP_SCRIPT -Raw
          if ($setup -notmatch '(?m)^#define MyAppExeName "LogPro\.exe"\s*$') {
            throw "publish/setup.iss must launch LogPro.exe, the published Windows entry point."
          }
          if ($setup -match 'QADeviceTool\.exe') {
            throw "publish/setup.iss contains the stale QADeviceTool.exe entry point."
          }

      - name: Prepare Windows app package
        shell: pwsh
        run: |
          function Test-ToolsManifest([string] $toolsRoot, [string] $manifestPath) {
            if (-not (Test-Path -LiteralPath $toolsRoot -PathType Container)) {
              throw "Bundled tools directory is missing: $toolsRoot"
            }
            $files = @(Get-ChildItem -LiteralPath $toolsRoot -File -Recurse)
            if ($files.Count -eq 0) { throw "Bundled tools directory is empty: $toolsRoot" }
            foreach ($file in $files) {
              $firstLine = Get-Content -LiteralPath $file.FullName -TotalCount 1 -ErrorAction SilentlyContinue
              if ($firstLine -eq "version https://git-lfs.github.com/spec/v1") {
                throw "Git LFS pointer was packaged instead of the real tool: $($file.FullName)"
              }
            }
            foreach ($name in @("adb.exe", "scrcpy.exe", "pymobiledevice3.exe")) {
              if (-not (Get-ChildItem -LiteralPath $toolsRoot -File -Recurse -Filter $name)) {
                throw "Required bundled tool is missing: $name"
              }
            }
            if (-not (Test-Path -LiteralPath $manifestPath -PathType Leaf)) {
              throw "Bundled tools manifest is missing: $manifestPath"
            }

            $root = (Resolve-Path -LiteralPath $toolsRoot).Path
            $entries = @(Get-Content -LiteralPath $manifestPath -Raw | ConvertFrom-Json)
            $expected = @{}
            foreach ($entry in $entries) {
              if ($expected.ContainsKey($entry.path)) { throw "Duplicate path in tools manifest: $($entry.path)" }
              $expected[$entry.path] = $entry.sha256.ToLowerInvariant()
            }
            $seen = @{}
            foreach ($file in $files) {
              $relative = $file.FullName.Substring($root.Length + 1).Replace('\', '/')
              $hash = (Get-FileHash -LiteralPath $file.FullName -Algorithm SHA256).Hash.ToLowerInvariant()
              if (-not $expected.ContainsKey($relative)) { throw "Unlisted bundled tool file: $relative" }
              if ($expected[$relative] -ne $hash) { throw "Bundled tool hash mismatch: $relative" }
              $seen[$relative] = $true
            }
            if ($seen.Count -ne $expected.Count) { throw "Tools manifest contains missing files." }
          }

          foreach ($notice in @("LogPro-LICENSE.txt", "LogPro-SECURITY.md", "LogPro-GPL_COMPLIANCE.md", "LogPro-THIRD_PARTY_NOTICES.txt", "SOURCE-OFFER-pymobiledevice3.txt")) {
            if (-not (Test-Path -LiteralPath "publish/app/licenses/$notice" -PathType Leaf)) {
              throw "Windows app package is missing legal notice: $notice"
            }
          }
          Test-ToolsManifest "publish/app/tools" "publish/app/tools-manifest.json"

      - name: Publish CLI
        run: dotnet publish ${{ env.CLI_PROJECT }} -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:Version=${{ steps.version.outputs.semver }} -o publish/cli

      - name: Prepare Windows CLI package
        shell: pwsh
        run: |
          $legal = "publish/cli/licenses"
          foreach ($notice in @("LogPro-LICENSE.txt", "LogPro-SECURITY.md", "LogPro-GPL_COMPLIANCE.md", "LogPro-THIRD_PARTY_NOTICES.txt", "SOURCE-OFFER-pymobiledevice3.txt")) {
            if (-not (Test-Path -LiteralPath (Join-Path $legal $notice) -PathType Leaf)) {
              throw "Windows CLI package is missing legal notice: $notice"
            }
          }

          $toolsRoot = "publish/cli/tools"
          $files = @(Get-ChildItem -LiteralPath $toolsRoot -File -Recurse)
          if ($files.Count -eq 0) { throw "Windows CLI package has no bundled tools." }
          foreach ($file in $files) {
            $firstLine = Get-Content -LiteralPath $file.FullName -TotalCount 1 -ErrorAction SilentlyContinue
            if ($firstLine -eq "version https://git-lfs.github.com/spec/v1") {
              throw "Git LFS pointer was packaged instead of the real CLI tool: $($file.FullName)"
            }
          }
          $manifestPath = "publish/cli/tools-manifest.json"
          if (-not (Test-Path -LiteralPath $manifestPath -PathType Leaf)) { throw "Windows CLI tools manifest is missing." }
          $root = (Resolve-Path -LiteralPath $toolsRoot).Path
          $entries = @(Get-Content -LiteralPath $manifestPath -Raw | ConvertFrom-Json)
          $expected = @{}
          foreach ($entry in $entries) { $expected[$entry.path] = $entry.sha256.ToLowerInvariant() }
          $seen = @{}
          foreach ($file in $files) {
            $relative = $file.FullName.Substring($root.Length + 1).Replace('\', '/')
            $hash = (Get-FileHash -LiteralPath $file.FullName -Algorithm SHA256).Hash.ToLowerInvariant()
            if (-not $expected.ContainsKey($relative) -or $expected[$relative] -ne $hash) { throw "CLI tool hash mismatch: $relative" }
            $seen[$relative] = $true
          }
          if ($seen.Count -ne $expected.Count) { throw "CLI tools manifest contains missing files." }

      - name: Remove debug symbols from release payloads
        shell: pwsh
        run: |
          Get-ChildItem publish/app, publish/cli -File -Recurse -Filter *.pdb | Remove-Item -Force

      - name: Upload App
        uses: actions/upload-artifact@v7
        with:
          name: app-published
          path: publish/app/

      - name: Upload CLI
        uses: actions/upload-artifact@v7
        with:
          name: cli-published
          path: publish/cli/

  installer:
    name: Setup.exe
    needs: build
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Download App
        uses: actions/download-artifact@v8
        with:
          name: app-published
          path: publish/app/

      - name: Validate installer input
        shell: pwsh
        run: |
          if (-not (Test-Path -LiteralPath "publish/app/LogPro.exe" -PathType Leaf)) {
            throw "Installer input is missing publish/app/LogPro.exe."
          }
          if (-not (Test-Path -LiteralPath "publish/app/tools-manifest.json" -PathType Leaf)) {
            throw "Installer input is missing the bundled-tools integrity manifest."
          }

      - name: Install Inno Setup
        run: choco install innosetup --no-progress -y

      - name: Inject Version & Compile
        shell: pwsh
        run: |
          $ver = "${{ needs.build.outputs.version }}"
          $iss = Get-Content "${{ env.SETUP_SCRIPT }}" -Raw
          $iss = $iss -replace '#define MyAppVersion "[^"]*"', "#define MyAppVersion `"$ver`""
          Set-Content "${{ env.SETUP_SCRIPT }}" -Value $iss
          Write-Host "Compiling installer for v$ver..."
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" "${{ env.SETUP_SCRIPT }}"
          $expected = "dist/LogPro_v$ver.exe"
          if (-not (Test-Path -LiteralPath $expected -PathType Leaf)) {
            throw "Inno Setup did not produce the expected installer: $expected"
          }

      - name: Sign Setup.exe
        if: env.SIGN_CERT_PFX != '' && env.SIGN_CERT_PASSWORD != ''
        shell: pwsh
        env:
          SIGN_CERT_PFX: ${{ secrets.SIGN_CERT_PFX }}
          SIGN_CERT_PASSWORD: ${{ secrets.SIGN_CERT_PASSWORD }}
        run: |
          $cert = "$env:RUNNER_TEMP\signing.pfx"
          [IO.File]::WriteAllBytes($cert, [Convert]::FromBase64String($env:SIGN_CERT_PFX))
          $signtool = (Get-ChildItem "C:\Program Files (x86)\Windows Kits\10\bin\*\x64\signtool.exe" | Select-Object -Last 1).FullName
          $exe = Get-Item (Get-ChildItem dist\*.exe | Select-Object -First 1).FullName
          & $signtool sign /fd SHA256 /f $cert /p $env:SIGN_CERT_PASSWORD /tr https://timestamp.digicert.com /td SHA256 $exe.FullName
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Upload Setup.exe
        uses: actions/upload-artifact@v7
        with:
          name: setup-exe
          path: dist/*.exe

  portable:
    name: Portable.zip
    needs: build
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Download App
        uses: actions/download-artifact@v8
        with:
          name: app-published
          path: publish/app/

      - name: Download CLI
        uses: actions/download-artifact@v8
        with:
          name: cli-published
          path: publish/cli/

      - name: Create Archives
        shell: pwsh
        run: |
          $ver = "${{ needs.build.outputs.version }}"
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Compress-Archive -Path publish/app/* -DestinationPath "dist/LogPro_v${ver}_portable.zip"
          Compress-Archive -Path publish/cli/* -DestinationPath "dist/logpro-cli_v${ver}_win-x64.zip"
          Get-ChildItem dist/

      - name: Upload Zips
        uses: actions/upload-artifact@v7
        with:
          name: portable-zip
          path: dist/*.zip

  macos:
    name: macOS.zip
    needs: build
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          lfs: true

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '10.0.x'

      - name: Publish macOS
        run: dotnet publish src/LogPro.Avalonia/LogPro.Avalonia.csproj -c Release -r osx-arm64 --self-contained true -p:PublishSingleFile=false -p:Version=${{ needs.build.outputs.version }} -o publish/mac

      - name: Validate macOS notices
        shell: bash
        run: |
          for file in LogPro-LICENSE.txt LogPro-SECURITY.md LogPro-GPL_COMPLIANCE.md LogPro-THIRD_PARTY_NOTICES.txt SOURCE-OFFER-pymobiledevice3.txt; do
            test -f "publish/mac/licenses/$file" || { echo "Missing macOS notice: $file"; exit 1; }
          done

      - name: Create macOS Archive
        shell: bash
        run: |
          mkdir -p dist
          cd publish/mac
          zip -r "../../dist/LogPro_v${{ needs.build.outputs.version }}_macos-arm64.zip" . -x "*.pdb"
          ls ../../dist/

      - name: Upload macOS.zip
        uses: actions/upload-artifact@v7
        with:
          name: macos-zip
          path: dist/*.zip

  linux:
    name: Linux.zip
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          lfs: true

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: '10.0.x'

      - name: Publish Linux
        run: dotnet publish src/LogPro.Avalonia/LogPro.Avalonia.csproj -c Release -r linux-x64 --self-contained true -p:PublishSingleFile=false -p:Version=${{ needs.build.outputs.version }} -o publish/linux

      - name: Validate Linux notices
        shell: bash
        run: |
          for file in LogPro-LICENSE.txt LogPro-SECURITY.md LogPro-GPL_COMPLIANCE.md LogPro-THIRD_PARTY_NOTICES.txt SOURCE-OFFER-pymobiledevice3.txt; do
            test -f "publish/linux/licenses/$file" || { echo "Missing Linux notice: $file"; exit 1; }
          done

      - name: Create Linux Archive
        shell: bash
        run: |
          mkdir -p dist
          cd publish/linux
          tar --exclude='*.pdb' -czf "../../dist/LogPro_v${{ needs.build.outputs.version }}_linux-x64.tar.gz" .
          ls ../../dist/

      - name: Upload Linux Archive
        uses: actions/upload-artifact@v7
        with:
          name: linux-zip
          path: dist/*.tar.gz

  release:
    name: GitHub Release
    if: (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch'
    needs: [build, installer, portable, macos, linux]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Download Installer
        uses: actions/download-artifact@v8
        with:
          name: setup-exe
          path: dist/

      - name: Download Zips
        uses: actions/download-artifact@v8
        with:
          name: portable-zip
          path: dist/

      - name: Download macOS.zip
        uses: actions/download-artifact@v8
        with:
          name: macos-zip
          path: dist/

      - name: Download Linux Archive
        uses: actions/download-artifact@v8
        with:
          name: linux-zip
          path: dist/

      - name: List Assets
        shell: bash
        run: |
          echo "=== Release Assets ==="
          ls -la dist/

      - name: Generate Changelog
        shell: bash
        run: |
          PREV=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          echo "## LogPro v${{ needs.build.outputs.version }}" > CHANGELOG.md
          echo "" >> CHANGELOG.md
          if [ -n "$PREV" ]; then
            git log "$PREV..HEAD" --pretty=format:"- %s" --no-merges >> CHANGELOG.md
          else
            git log --pretty=format:"- %s" --no-merges >> CHANGELOG.md
          fi
          cat CHANGELOG.md

      - name: Create Release with Assets
        uses: softprops/action-gh-release@v3
        with:
          tag_name: v${{ needs.build.outputs.version }}
          name: LogPro v${{ needs.build.outputs.version }}
          body_path: CHANGELOG.md
          files: dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
